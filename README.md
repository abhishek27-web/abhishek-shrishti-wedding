# Abhishek & Shrishti — Wedding Invitation

This version is **self-contained**: all invitation images are embedded inside `index.html`.
You only need to upload `index.html` to the root of a GitHub Pages repository.
